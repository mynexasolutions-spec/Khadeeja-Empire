import { getDataProvider } from "@/lib/data";
import { deleteReviewAction, updateReviewAction } from "@/actions/admin/reviews";
import { AdminCard, EmptyState, PageHeading, StatusBadge, formatDate } from "../_components/AdminPage";
export const dynamic = "force-dynamic";
export default async function ReviewsPage() { const reviews=await getDataProvider().listReviews(); return <div><PageHeading title="Reviews" description="Moderate product reviews and choose homepage highlights."/>{reviews.length===0?<AdminCard><EmptyState title="No reviews awaiting moderation"/></AdminCard>:<div className="space-y-4">{reviews.map((r)=><AdminCard key={r.id} className="p-5"><div className="flex flex-col gap-3 sm:flex-row sm:justify-between"><div><div className="flex items-center gap-2"><h2 className="font-semibold text-stone-900">{r.title||`${r.rating}-star review`}</h2><StatusBadge value={r.status}/></div><p className="mt-1 text-sm text-stone-500">{r.authorName} · {formatDate(r.createdAt)}</p></div><span className="text-amber-500" aria-label={`${r.rating} stars`}>{"★".repeat(r.rating)}{"☆".repeat(5-r.rating)}</span></div><p className="mt-4 text-sm leading-6 text-stone-700">{r.body}</p><div className="mt-4 flex flex-wrap gap-2 border-t border-stone-100 pt-4">{["pending","approved","rejected"].map(status=><form action={updateReviewAction} key={status}><input type="hidden" name="id" value={r.id}/><input type="hidden" name="status" value={status}/><input type="hidden" name="isHomeFeatured" value={String(r.isHomeFeatured===true)}/><button className="rounded-md border border-stone-300 px-3 py-1.5 text-xs font-semibold capitalize">{status}</button></form>)}<form action={updateReviewAction}><input type="hidden" name="id" value={r.id}/><input type="hidden" name="status" value={r.status||"pending"}/><input type="hidden" name="isHomeFeatured" value={String(!r.isHomeFeatured)}/><button className="rounded-md border border-stone-300 px-3 py-1.5 text-xs font-semibold">{r.isHomeFeatured?"Remove from home":"Feature on home"}</button></form><form action={deleteReviewAction} className="sm:ml-auto"><input type="hidden" name="id" value={r.id}/><button className="rounded-md border border-red-200 px-3 py-1.5 text-xs font-semibold text-red-700">Delete</button></form></div></AdminCard>)}</div>}</div>; }

