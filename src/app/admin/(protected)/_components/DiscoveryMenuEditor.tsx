import type { CategoryRecord, DiscoveryMenuEntryRecord } from "@/lib/admin/types";
import { deleteDiscoveryMenuEntryAction, saveDiscoveryMenuEntryAction } from "@/actions/admin/settings";
const input="min-h-10 rounded-lg border border-stone-300 bg-white px-3 text-sm outline-none focus:border-[#9c5247]";
export function DiscoveryMenuEditor({entries,categories}:{entries:DiscoveryMenuEntryRecord[];categories:CategoryRecord[]}){return <div><h2 className="font-semibold text-stone-900">Discovery menu</h2><p className="mt-1 text-sm text-stone-500">Links displayed in the storefront discovery navigation.</p><div className="mt-4 space-y-2">{entries.map(entry=><div key={entry.id} className="flex flex-col gap-2 rounded-lg border border-stone-200 p-3 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-sm font-semibold text-stone-900">{entry.label}</p><p className="text-xs text-stone-500">{entry.href} · Order {entry.sortOrder??0}</p></div><form action={deleteDiscoveryMenuEntryAction}><input type="hidden" name="id" value={entry.id}/><button className="text-left text-xs font-semibold text-red-700">Delete</button></form></div>)}</div><form action={saveDiscoveryMenuEntryAction} className="mt-4 grid gap-2 sm:grid-cols-2 lg:grid-cols-4"><input type="hidden" name="active" value="true"/><input type="hidden" name="sortOrder" value={entries.length}/><input className={input} name="label" required placeholder="Link label"/><input className={input} name="href" required placeholder="/collections/..."/><select className={input} name="categoryId"><option value="">No category</option>{categories.map(category=><option key={category.id} value={category.id}>{category.name}</option>)}</select><button className="rounded-lg bg-[#9c5247] px-4 text-sm font-semibold text-white">Add link</button></form></div>}
