import { notFound } from "next/navigation";
import { getDataProvider } from "@/lib/data";
import { deleteOrderAction, updateOrderStatusAction } from "@/actions/admin/orders";
import { AdminCard, EmptyState, PageHeading, StatusBadge, formatCurrency, formatDate, tableCell, tableHead } from "../../_components/AdminPage";
import { ConfirmDeleteButton } from "../../_components/ConfirmDeleteButton";
export default async function OrderDetailPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const provider = getDataProvider(); const order = await provider.getOrder(id); if (!order) notFound(); const items = order.items?.length ? order.items : await provider.listOrderItems(order.id); const address = order.shippingAddress; return <div><PageHeading title={`Order #${order.orderNumber}`} description={`Placed ${formatDate(order.createdAt)}`}/><div className="grid gap-6 xl:grid-cols-[1fr_340px]"><div className="space-y-6"><AdminCard><div className="border-b border-stone-100 px-5 py-4"><h2 className="font-semibold">Order items</h2></div>{items.length === 0 ? <EmptyState title="No items attached"/> : <div className="overflow-x-auto"><table className="w-full"><thead className="bg-stone-50"><tr><th className={tableHead}>Item</th><th className={tableHead}>Options</th><th className={tableHead}>Price</th><th className={tableHead}>Qty</th><th className={tableHead}>Total</th></tr></thead><tbody className="divide-y divide-stone-100">{items.map((item) => <tr key={item.id}><td className={tableCell}><p className="font-semibold text-stone-900">{item.productName}</p><p className="text-xs text-stone-400">{item.productSlug || item.productId || ""}</p></td><td className={tableCell}>{[item.size,item.color].filter(Boolean).join(" / ") || "—"}</td><td className={tableCell}>{formatCurrency(item.unitPrice, order.currency || "INR")}</td><td className={tableCell}>{item.quantity}</td><td className={tableCell}>{formatCurrency(item.totalPrice, order.currency || "INR")}</td></tr>)}</tbody></table></div>}</AdminCard><AdminCard className="p-5"><h2 className="mb-3 font-semibold">Shipping address</h2>{address ? <address className="text-sm not-italic leading-6 text-stone-600">{address.fullName}<br/>{address.line1}{address.line2 ? <><br/>{address.line2}</> : null}<br/>{address.city}, {address.state} {address.postalCode}<br/>{address.country || "IN"}{address.phone ? <><br/>{address.phone}</> : null}</address> : <p className="text-sm text-stone-400">No address recorded.</p>}</AdminCard></div><aside className="space-y-6"><AdminCard className="p-5"><h2 className="mb-4 font-semibold">Status</h2><form action={updateOrderStatusAction} className="space-y-3"><input type="hidden" name="id" value={order.id}/><select name="status" defaultValue={order.status} className="min-h-11 w-full rounded-lg border border-stone-300 px-3 text-sm">{["pending","confirmed","processing","shipped","delivered","cancelled","refunded"].map((status) => <option key={status} value={status}>{status}</option>)}</select><button className="min-h-10 w-full rounded-lg bg-[#9c5247] px-4 text-sm font-semibold text-white">Update status</button></form><div className="mt-4 flex justify-between border-t border-stone-100 pt-4 text-sm"><span>Payment</span><StatusBadge value={order.paymentStatus}/></div></AdminCard><AdminCard className="p-5"><h2 className="mb-4 font-semibold">Payment summary</h2><dl className="space-y-2 text-sm"><div className="flex justify-between"><dt>Subtotal</dt><dd>{formatCurrency(order.subtotal)}</dd></div><div className="flex justify-between"><dt>Shipping</dt><dd>{formatCurrency(order.shipping)}</dd></div><div className="flex justify-between"><dt>Discount</dt><dd>-{formatCurrency(order.discount)}</dd></div><div className="flex justify-between border-t border-stone-100 pt-3 font-bold"><dt>Total</dt><dd>{formatCurrency(order.total)}</dd></div></dl></AdminCard><ConfirmDeleteButton action={deleteOrderAction} id={order.id} confirmMessage="Delete this order? This cannot be undone." successMessage="Order deleted." redirectTo="/admin/orders" label="Delete order"/></aside></div></div>; }

