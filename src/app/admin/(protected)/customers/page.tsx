import { getDataProvider } from "@/lib/data";
import { AdminCard, EmptyState, PageHeading, StatusBadge, formatCurrency, formatDate, tableCell, tableHead } from "../_components/AdminPage";
export const dynamic = "force-dynamic";
export default async function CustomersPage({ searchParams }: { searchParams: Promise<{ q?: string }> }) { const { q } = await searchParams; const customers = await getDataProvider().listCustomers({ search: q }); const active = customers.filter((c) => c.status !== "inactive").length; return <div><PageHeading title="Customers" description={`${customers.length} customers · ${active} active`}/><form className="mb-4 flex max-w-md gap-2"><input aria-label="Search customers" className="min-h-10 flex-1 rounded-lg border border-stone-300 bg-white px-3 text-sm" name="q" defaultValue={q || ""} placeholder="Name, phone or email..."/><button className="rounded-lg border border-stone-300 bg-white px-4 text-sm font-semibold">Search</button></form><AdminCard>{customers.length === 0 ? <EmptyState title="No customers found" detail="Phone sign-ins and checkout customers appear here."/> : <div className="overflow-x-auto"><table className="w-full"><thead className="bg-stone-50"><tr><th className={tableHead}>Customer</th><th className={tableHead}>Contact</th><th className={tableHead}>Orders</th><th className={tableHead}>Spent</th><th className={tableHead}>Status</th><th className={tableHead}>Joined</th></tr></thead><tbody className="divide-y divide-stone-100">{customers.map((customer) => <tr key={customer.id}><td className={tableCell}><p className="font-semibold text-stone-900">{customer.name || "Guest customer"}</p></td><td className={tableCell}><p>{customer.phone || "—"}</p><p className="text-xs text-stone-400">{customer.email || ""}</p></td><td className={tableCell}>{customer.orderCount || 0}</td><td className={tableCell}>{formatCurrency(customer.totalSpent || 0)}</td><td className={tableCell}><StatusBadge value={customer.status || "active"}/></td><td className={tableCell}>{formatDate(customer.createdAt)}</td></tr>)}</tbody></table></div>}</AdminCard></div>; }
